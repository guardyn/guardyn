name: Desktop CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'client-desktop/**'
      - 'backend/crates/crypto/**'
      - 'backend/crates/common/**'
      - '.github/workflows/desktop-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'client-desktop/**'
      - 'backend/crates/crypto/**'
      - 'backend/crates/common/**'
      # Without this the workflow cannot run on a PR that only edits the workflow, so a
      # change to desktop CI was verified solely after merge. The push trigger above has
      # carried the self-reference all along; the pull_request one was simply missing it.
      - '.github/workflows/desktop-ci.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  NODE_VERSION: '20'

# CI/CD trigger: updated Linux dependencies for Tauri v2

jobs:
  # Task 17: Test Job
  test:
    name: Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client-desktop/package-lock.json

      - name: Install dependencies
        working-directory: client-desktop
        run: npm ci

      - name: Run tests with coverage
        working-directory: client-desktop
        run: npx vitest run --coverage --reporter=verbose

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: client-desktop/coverage
          if-no-files-found: warn

  # Task 19: Linting CI Check  
  lint:
    name: Lint
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client-desktop/package-lock.json

      - name: Install dependencies
        working-directory: client-desktop
        run: npm ci

      - name: Run ESLint
        working-directory: client-desktop
        run: npm run lint

      - name: Run TypeScript check
        working-directory: client-desktop
        run: npx tsc --noEmit

  # Task 16: Vitest Coverage >80%
  coverage-check:
    name: Coverage Check
    runs-on: ubuntu-latest
    needs: test
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client-desktop/package-lock.json

      - name: Install dependencies
        working-directory: client-desktop
        run: npm ci

      - name: Run tests with coverage threshold
        working-directory: client-desktop
        run: npx vitest run --coverage --coverage.thresholds.lines=70 --coverage.thresholds.functions=70 --coverage.thresholds.branches=60
        # Measured 43.33% lines / 52.51% functions against a declared 70% bar. The
        # thresholds are aspirational, not met - #192 either lowers them to the real
        # figure and ratchets up, or adds the missing tests. Until then this step
        # cannot be allowed to fail the build.
        continue-on-error: true

  # Rust tests
  rust-test:
    name: Rust Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            curl \
            wget \
            file \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            protobuf-compiler

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            client-desktop/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client-desktop/package-lock.json

      - name: Install frontend dependencies
        working-directory: client-desktop
        run: npm ci

      - name: Build frontend
        working-directory: client-desktop
        run: npm run build

      - name: Run Rust tests
        working-directory: client-desktop/src-tauri
        run: cargo test --all-features

      - name: Run Clippy
        working-directory: client-desktop/src-tauri
        run: cargo clippy -- -D warnings
        # 132 errors today, 95 of them result_large_err. Tracked as #191; the step
        # stays non-blocking until that lands, and must become blocking when it does.
        continue-on-error: true

  # Task 18: Linux Build
  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    needs: [test, lint, rust-test]
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client-desktop/package-lock.json

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            curl \
            wget \
            file \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            protobuf-compiler

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            client-desktop/target/
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}

      - name: Install frontend dependencies
        working-directory: client-desktop
        run: npm ci

      - name: Build frontend
        working-directory: client-desktop
        run: npm run build

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: client-desktop
          tauriScript: npm run tauri
          args: --target x86_64-unknown-linux-gnu

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v7
        with:
          name: guardyn-desktop-linux-x64
          path: |
            client-desktop/target/x86_64-unknown-linux-gnu/release/bundle/**/*.deb
            client-desktop/target/x86_64-unknown-linux-gnu/release/bundle/**/*.AppImage
          # A build that produces no installer is a failed build. `warn` reported that as
          # success for as long as the path above was wrong, which is how the defect survived.
          if-no-files-found: error
          retention-days: 30
